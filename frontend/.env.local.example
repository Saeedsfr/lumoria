# frontend/.env.local — این فایل را به .env.local تغییر نام بده
# gitignore شده، secret نگه داش

# کلید API تنسنتر (برای خواندن موجودی SAP از blockchain)
VITE_TON_API_KEY=

# آدرس deploy شده اپ (برای manifest تنکانکت)
VITE_APP_URL=https://your-app.vercel.app
